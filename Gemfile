source "https://rubygems.org"

# Use GitHub Pages gem, which includes Jekyll and supported plugins
gem "github-pages", group: :jekyll_plugins

# Windows dependency (optional, only if you are on Windows)
gem "wdm", "~> 0.1.0" if Gem.win_platform?

